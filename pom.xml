<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.eric.labo1</groupId>
    <artifactId>labo1-jakartaee</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <name>Labo1 - Jakarta EE</name>
    <description>Laboratoire 1 - Programmation Web Serveur III (420-140-GG)</description>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Versions centralisees : jamais de LATEST/RELEASE (build non reproductible) -->
        <jakartaee.version>10.0.0</jakartaee.version>
        <postgresql.version>42.7.13</postgresql.version>
        <junit.version>5.14.4</junit.version>
    </properties>

    <dependencies>

        <!-- ============================================================
             API Jakarta EE 10 : namespace jakarta.* (PAS javax.*)
             scope = provided -> l'API est deja fournie par Tomcat 10.1+
             a l'execution. L'embarquer dans le .war provoquerait des
             conflits de chargement de classes.
             ============================================================ -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-api</artifactId>
            <version>${jakartaee.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- ============================================================
             Pilote JDBC du SGBD.
             scope par defaut (compile) -> il DOIT etre embarque dans le
             .war, car Tomcat ne fournit aucun pilote de base de donnees.
             ============================================================ -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
        </dependency>

        <!--
             Variante MySQL (si tu utilises MySQL plutot que PostgreSQL) :
             commente le bloc postgresql ci-dessus et decommente celui-ci.

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.1.0</version>
        </dependency>
        -->

        <!-- Tests unitaires -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <finalName>labo1</finalName>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.5.1</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
            </plugin>

        </plugins>
    </build>

</project>
